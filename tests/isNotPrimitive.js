/*global require, describe, it */

(function () {
    'use strict';

    var required = require('../scripts/'),
        utilx = required.utilx,
        expect = required.expect;

    /*
    describe('isNotPrimitive', function () {
        it('should not throw an error in each case', function () {
            expect(utilx.isNotPrimitive()).to.not.be.ok();
            expect(utilx.isNotPrimitive(null)).to.not.be.ok();
            expect(utilx.isNotPrimitive('')).to.not.be.ok();
            expect(utilx.isNotPrimitive(1)).to.not.be.ok();
            expect(utilx.isNotPrimitive(false)).to.not.be.ok();
            expect(utilx.isNotPrimitive({})).to.be.ok();
            expect(utilx.isNotPrimitive([])).to.be.ok();
        });
    });
    */
}());
